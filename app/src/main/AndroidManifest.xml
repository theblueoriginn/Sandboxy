<?xml version="1.0" encoding="UTF-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET" />
    <application android:icon="@mipmap/icl" android:usesCleartextTraffic="true" android:name=".MyApp">
<activity android:name=".MainScreen" android:exported="true" android:theme="@style/Theme.AppCompat.Light"  android:label="@string/app_name">
    <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter>
</activity>
        <activity android:name=".SignIn" android:theme="@style/Theme.AppCompat.Light" android:label="Login"></activity>
        <activity android:name=".HomePage"></activity>
        <activity android:name=".ActivityScratch" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND"></action>
                <category android:name="android.intent.category.DEFAULT"></category>
                <data android:mimeGroup="@string/app_name"></data>
            </intent-filter>
        </activity>
<activity android:name=".VerifyMail"></activity>
    </application>

</manifest>